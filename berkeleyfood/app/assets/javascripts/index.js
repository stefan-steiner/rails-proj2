$(document).ready(function() {
  //Chicken Tenders
  $('#quantity-increase0').click(function() {
    var quantity = $('#number0').text();
    var num = parseInt(quantity);
    $('#number0').text(num + 1);
  });

  $('#quantity-decrease0').click(function() {
    var quantity = $('#number0').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number0').text(num - 1);
    }
  });
  //Chicken Quesadilla //
  $('#quantity-increase1').click(function() {
    var quantity = $('#number1').text();
    var num = parseInt(quantity);
    $('#number1').text(num + 1);
  });

  $('#quantity-decrease1').click(function() {
    var quantity = $('#number1').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number1').text(num - 1);
    }
  });

  //Wings //
  $('#quantity-increase2').click(function() {
    var quantity = $('#number2').text();
    var num = parseInt(quantity);
    $('#number2').text(num + 1);
  });

  $('#quantity-decrease2').click(function() {
    var quantity = $('#number2').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number2').text(num - 1);
    }
  });

  //Orange Chicken Plate //
  $('#quantity-increase3').click(function() {
    var quantity = $('#number3').text();
    var num = parseInt(quantity);
    $('#number3').text(num + 1);
  });

  $('#quantity-decrease3').click(function() {
    var quantity = $('#number3').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number3').text(num - 1);
    }
  });

  //Steak Plate //
  $('#quantity-increase4').click(function() {
    var quantity = $('#number4').text();
    var num = parseInt(quantity);
    $('#number4').text(num + 1);
  });

  $('#quantity-decrease4').click(function() {
    var quantity = $('#number4').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number4').text(num - 1);
    }
  });

  //Hamburger //
  $('#quantity-increase5').click(function() {
    var quantity = $('#number5').text();
    var num = parseInt(quantity);
    $('#number5').text(num + 1);
  });

  $('#quantity-decrease5').click(function() {
    var quantity = $('#number5').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number5').text(num - 1);
    }
  });

  //Chocolate Pancakes //
  $('#quantity-increase6').click(function() {
    var quantity = $('#number6').text();
    var num = parseInt(quantity);
    $('#number6').text(num + 1);
  });

  $('#quantity-decrease6').click(function() {
    var quantity = $('#number6').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number6').text(num - 1);
    }
  });

  //Breakfast Burrito //
  $('#quantity-increase7').click(function() {
    var quantity = $('#number7').text();
    var num = parseInt(quantity);
    $('#number7').text(num + 1);
  });

  $('#quantity-decrease7').click(function() {
    var quantity = $('#number7').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number7').text(num - 1);
    }
  });

  //Tater tots //
  $('#quantity-increase8').click(function() {
    var quantity = $('#number8').text();
    var num = parseInt(quantity);
    $('#number8').text(num + 1);
  });

  $('#quantity-decrease8').click(function() {
    var quantity = $('#number8').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number8').text(num - 1);
    }
  });

  //French Fries //
  $('#quantity-increase9').click(function() {
    var quantity = $('#number9').text();
    var num = parseInt(quantity);
    $('#number9').text(num + 1);
  });

  $('#quantity-decrease9').click(function() {
    var quantity = $('#number9').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number9').text(num - 1);
    }
  });

  //Hot Dog//
  $('#quantity-increase10').click(function() {
    var quantity = $('#number10').text();
    var num = parseInt(quantity);
    $('#number10').text(num + 1);
  });

  $('#quantity-decrease10').click(function() {
    var quantity = $('#number10').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number10').text(num - 1);
    }
  });

  //Egg Omelette //
  $('#quantity-increase11').click(function() {
    var quantity = $('#number11').text();
    var num = parseInt(quantity);
    $('#number11').text(num + 1);
  });

  $('#quantity-decrease11').click(function() {
    var quantity = $('#number11').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number11').text(num - 1);
    }
  });

  //Egg and Ham Omelette //
  $('#quantity-increase12').click(function() {
    var quantity = $('#number12').text();
    var num = parseInt(quantity);
    $('#number12').text(num + 1);
  });

  $('#quantity-decrease12').click(function() {
    var quantity = $('#number12').text();
    var num = parseInt(quantity);
    if (num > 1) {
      $('#number12').text(num - 1);
    }
  });
});
